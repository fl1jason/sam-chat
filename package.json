{
  "name": "sam-chat",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "16.17.0"
  },
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^6.4.2",
    "@fortawesome/free-solid-svg-icons": "^6.4.2",
    "@fortawesome/react-fontawesome": "^0.2.0",
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "dayjs": "^1.11.9",
    "firebase": "^10.2.0",
    "react": "^17.0.1",
    "react-dayjs": "^0.3.2",
    "react-dom": "^17.0.1",
    "react-firebase-hooks": "^5.1.1",
    "react-hook-form": "^6.15.3",
    "react-scripts": "4.0.2",
    "react-toastify": "8.0.0",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "dev": "export NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
    "start": "export NODE_OPTIONS=--openssl-legacy-provider && node --max_old_space_size=2560 node_modules/.bin/react-scripts && react-scripts start",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && node --max_old_space_size=2560 node_modules/.bin/react-scripts && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
    "postcss": "^8.4.30"
  }
}